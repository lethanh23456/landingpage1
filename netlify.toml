[build]
  command = "npm install --legacy-peer-deps && npm install pdfjs-dist@2.2.228 --legacy-peer-deps && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  COREPACK_ENABLE = "0"
  NODE_OPTIONS = "--openssl-legacy-provider"